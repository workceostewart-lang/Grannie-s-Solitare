{
  "name": "grannies-solitare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "deploy": "wrangler deploy",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^8.1.3",
    "wrangler": "^4.110.0"
  }
}
